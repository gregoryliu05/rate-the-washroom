# Root environment variables for local development.
# - Used by `docker-compose.yml` variable substitution
# - Also a good reference for what needs to be set in your hosting provider

# =========================
# Backend (FastAPI)
# =========================

# Database (local/non-docker example)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/rate_the_washroom

# Firebase Admin SDK (SERVER-SIDE, secret)
# Do NOT commit the JSON file. Recommended local path (already gitignored):
#   apps/backend/firebaseAccountKey.json
# If running the backend outside Docker, set GOOGLE_APPLICATION_CREDS to your absolute path:
GOOGLE_APPLICATION_CREDS=/absolute/path/to/firebaseAccountKey.json

# CORS (JSON list recommended; comma-separated also supported)
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:3001"]

ENVIRONMENT=development
DEBUG=true
REDIS_URL=redis://localhost:6379


# =========================
# Frontend (Next.js)
# =========================

# API base URL seen by the browser
NEXT_PUBLIC_API_URL=http://localhost:8000

# Mapbox (PUBLIC token; restrict by domain + scopes in Mapbox)
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_mapbox_public_token

# Firebase Web SDK (PUBLIC config; restrict by domain in Firebase)
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_firebase_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id
